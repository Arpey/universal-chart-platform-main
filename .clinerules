# .clinerules

## 1. 代码风格
- 使用函数式组件（Vue/React 均适用，优先 Composition API / Hooks 写法）。
- 如果项目使用 TypeScript，所有代码必须使用 TypeScript，并补充必要的类型定义，避免 any 滥用。

## 2. 修改范围
- 未经明确允许，不得修改 `package.json`、`vite.config.js`（以及 `vite.config.ts`、`tsconfig.json` 等其他构建/配置文件）。
- 如需改动配置文件，必须先向用户说明并征求同意。

## 3. 回答风格
- 每次给出代码修改时，必须附带简要的改动说明，说明修改原因和影响。

## 4. 操作限制
- 执行任何文件写入或终端命令前，必须向用户请求批准。
- 用户明确批准后，方可执行相应操作。
