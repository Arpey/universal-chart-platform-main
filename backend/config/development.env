PORT=3001
CORS_ORIGIN=http://localhost:5173
HTTPS_PROXY=http://127.0.0.1:10808
# 说明：合约行情 WebSocket 使用 fstream.binance.com（config.ts 默认值），
# 如需覆盖请在 backend/.env 设置 BINANCE_FUTURES_WS_URL，勿指向现货流，否则 TradFi 合约无推送。

